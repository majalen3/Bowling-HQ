VITE_API_URL=http://localhost:8000/api/v1
VITE_APP_NAME=Bowling-HQ

# For Capacitor (iOS/Android) native builds, set this to the deployed backend URL.
# The app cannot reach localhost from a device; use your server's public address.
# Example: VITE_API_URL=https://api.yourdomain.com/api/v1
