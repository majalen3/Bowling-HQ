# Database

Database assets for local development live here.

## Structure

```text
database/
├── schemas/
│   ├── postgres.sql
│   └── mongodb.js
├── seeds/
└── migrations/
```

The PostgreSQL and MongoDB schema files are mounted into Docker containers for local initialization.
