name: Auto-approve GitHub Copilot CI runs

on:
  workflow_run:
    workflows:
      - CI
    types:
      - requested

permissions:
  actions: write
  contents: read

jobs:
  approve:
    # 198982749 is the stable GitHub user ID for the @copilot app account.
    # If GitHub ever changes this actor, verify via https://api.github.com/users/copilot and update this guard.
    if: ${{ github.event.workflow_run.actor.id == 198982749 && github.event.workflow_run.actor.type == 'Bot' && (github.event.workflow_run.actor.login == 'Copilot' || github.event.workflow_run.actor.login == 'copilot') }}
    runs-on: ubuntu-latest
    steps:
      - name: Approve Copilot-triggered CI run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eu
          echo "Approving run ${{ github.event.workflow_run.id }} in ${{ github.repository }}"
          gh api \
            --method POST \
            /repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}/approve
          echo "Approval request submitted."
